请在每次回复前使用 caveman skill, 使用 lite 模式.

项目版本号应当遵循语义化版本 `X.Y.Z`：
- `Z` (patch)：向后兼容的修复与微小改动。例如：缺陷修复、文档更新、注释修正、内部实现调整等，不改变公共行为与接口。
- `Y` (minor)：向后兼容的新增功能或增强。例如：新增可选能力、新增模块/工具/依赖、扩展配置项、添加内置数据等，不影响现有使用方式。
- `X` (major)：不向后兼容的破坏性变更。例如：删除或重命名公共接口、修改 API/协议、改变输出 schema/数据契约、移动关键路径或配置、删除依赖等。

每次改动后，使用 Git 管理项目版本，并遵循以下规则：
1. 保持 main 分支干净。
2. 根据改动类型选择提交与分支策略：
   - 文档改动：无需创建新分支，提交 commit 时添加 `(docs)` 前缀，例如 `(docs) <commit_message>`。
   - Bug 修复：创建 `fix/<branch_name>` 分支。
   - 新增功能：创建 `feat/<branch_name>` 分支。
   - 优化或重构（不改变原功能）：创建 `optim/<branch_name>` 或 `refac/<branch_name>` 分支。
3. 合并分支时始终使用 `--no-ff` 参数。
4. 合并前需要询问用户得到许可

使用 browser-use skill 以及 qa skill 来完成前端应用的测试与评估。